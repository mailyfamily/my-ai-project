services:
  - type: web
    name: german-listening-practice
    env: node
    plan: free
    buildCommand: cd backend && npm install && cd ../frontend && npm install --production=false && npm run build
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
