services:
  - type: web
    name: german-listening-practice
    env: node
    plan: free
    buildCommand: bash build.sh
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: development
